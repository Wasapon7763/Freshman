#include<stdio.h>
int main(void){
  int y;
  printf("Input the Anno Domini(A.D.)-> ");
  scanf("%d",&y);
  if((y%4==0 && y%100!=0)||(y%400=0)){
    printf("A.D. %d is a leap year \n",y);
  }
  else{
    printf("A.D. %d is not a leap year \n",y);
  }
  return 0;
}
